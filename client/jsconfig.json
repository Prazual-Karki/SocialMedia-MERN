// compile the src directory and all its contents.
//when you import different files from other files,you can just start from src
{
    "compilerOptions": {
      "baseUrl": "src"
    },
    "include": ["src"]
  }